# Normalise line endings so the repo stays LF regardless of who clones it.
* text=auto eol=lf

# Binary assets Git should never try to diff or convert.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.mp4 binary
*.pdf binary
*.woff2 binary
